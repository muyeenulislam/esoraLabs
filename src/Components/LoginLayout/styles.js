import Colors from "@/utils/Colors";

const styles = {
  container: "w-full flex flex-col",
  logoContainer: "flex justify-center py-6",
  valueContainer: `h-full mb-[80px]  rounded-[50px] bg-[${Colors.LightBlue}] text-[${Colors.GreyishBlue} relative overflow-hidden`,
};
export default styles;
